{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notas",
  "description": "Permissions for the notes reader window",
  "windows": ["notas"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
